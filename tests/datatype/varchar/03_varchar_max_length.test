# Test Case Name: VARCHAR类型最大长度测试
# Description: 测试VARCHAR类型的最大长度限制
# Category: datatype/varchar

statement ok
DROP TABLE IF EXISTS test_varchar_max_length;

statement ok
CREATE TABLE test_varchar_max_length (id INTEGER PRIMARY KEY, long_text VARCHAR(8188));

statement ok
INSERT INTO test_varchar_max_length VALUES (1, REPEAT('A', 8000));

query II
SELECT id, LENGTH(long_text) FROM test_varchar_max_length;
----
1 8000

statement ok
DROP TABLE test_varchar_max_length;
