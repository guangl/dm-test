# Test Case Name: CHAR类型约束测试
# Description: 测试CHAR类型字段的唯一性和检查约束
# Category: datatype/char

statement ok
DROP TABLE IF EXISTS test_char_constraints;

statement ok
CREATE TABLE test_char_constraints (
    id INTEGER PRIMARY KEY,
    username CHAR(20) UNIQUE,
    status CHAR(10) CHECK (status IN ('ACTIVE    ', 'INACTIVE  '))
);

statement ok
INSERT INTO test_char_constraints VALUES (1, 'user001', 'ACTIVE');

statement error
INSERT INTO test_char_constraints VALUES (2, 'user001', 'ACTIVE');

statement error
INSERT INTO test_char_constraints VALUES (3, 'user002', 'PENDING');

statement ok
INSERT INTO test_char_constraints VALUES (4, 'user002', 'INACTIVE');

query ITT
SELECT * FROM test_char_constraints ORDER BY id;
----
1 user001              ACTIVE    
4 user002              INACTIVE  

statement ok
DROP TABLE test_char_constraints;
