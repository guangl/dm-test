# Test Case Name: ROWID子查询测试
# Description: 测试ROWID在子查询中的使用
# Category: datatype/rowid

statement ok
DROP TABLE IF EXISTS test_rowid_sub;

statement ok
CREATE TABLE test_rowid_sub (id INTEGER PRIMARY KEY, val INTEGER);

statement ok
INSERT INTO test_rowid_sub VALUES (1, 100), (2, 200);

query I
SELECT COUNT(*) FROM test_rowid_sub WHERE id IN (SELECT id FROM test_rowid_sub WHERE ROWID IS NOT NULL);
----
2

statement ok
DROP TABLE test_rowid_sub;
