# Test Case Name: 两个空字符串连接测试
# Description: 测试两个空字符串连接的结果
# Category: express/string_expressions
# Reference: https://eco.dameng.com/document/dm/zh-cn/pm/dm_sql-introduction.html#1.5.2

# 为什么突然挂了 - 可能是环境因素

statement ok
DROP TABLE IF EXISTS EMPTY_STRING_TEST;

statement ok
CREATE TABLE EMPTY_STRING_TEST (
    ID INT PRIMARY KEY,
    NAME VARCHAR(100)
);

statement ok
INSERT INTO EMPTY_STRING_TEST (ID, NAME) VALUES (1, '');

query T
SELECT NAME || '' AS CONCATENATED_NAME FROM EMPTY_STRING_TEST WHERE ID = 1;
----
<EMPTY>
